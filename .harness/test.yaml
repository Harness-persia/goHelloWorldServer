pipeline:
  name: test
  identifier: test
  projectIdentifier: Hardik_project
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: waveshare
        build: <+input>
  stages:
    - stage:
        name: Build
        identifier: Build
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Run_1
                  identifier: Run_1
                  spec:
                    shell: Sh
                    command: |-
                      cd wave-share && mkdir build && cd build
                      cmake ..
                      make

                      # running
                      ./wave-share
              - step:
                  type: BuildAndPushDockerRegistry
                  name: BuildAndPushDockerRegistry_1
                  identifier: BuildAndPushDockerRegistry_1
                  spec:
                    connectorRef: buildtest
                    repo: persia2309/harness-deploy-test
                    tags:
                      - deploy
